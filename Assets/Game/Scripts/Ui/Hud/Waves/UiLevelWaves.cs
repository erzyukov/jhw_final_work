namespace Game.Ui
{
	using Game.Utilities;
	using System;
	using Zenject;

	public class UiLevelWaves : ControllerBase, IInitializable
	{
		public void Initialize()
		{
			/*
			int wavesCount = _levelConfig.Waves.Length;
			_waves = new UiWaveView[wavesCount];
			_delimiters = new UiWaveDelimiterView[wavesCount - 1];


			for (int i = 0; i < wavesCount; i++)
			{
				_waves[i] = CreateWave();
			}

			*/
		}
	}
}
